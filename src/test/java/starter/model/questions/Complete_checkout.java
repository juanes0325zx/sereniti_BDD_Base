package starter.model.questions;

import net.serenitybdd.screenplay.Question;
import net.serenitybdd.screenplay.questions.TextContent;
import starter.model.pageObjects.saucedemo_page.saucedemo_complete_checkout;
import static net.serenitybdd.screenplay.matchers.WebElementStateMatchers.*;
import static net.serenitybdd.screenplay.questions.WebElementQuestion.the;

public class Complete_checkout {
/*
    public static Question<String> Span_complete() {
        return actor -> TextContent.of (saucedemo_complete_checkout.Span_complete).viewedBy (actor).asString ().trim ();
    }
    public static Question<String> btn_backToHome() {
        return actor -> TextContent.of (saucedemo_complete_checkout.btn_backToHome).viewedBy (actor).asString ().trim ();
    }
    public static Question<String> Info_text() {
        return actor -> TextContent.of (saucedemo_complete_checkout.Info_text).viewedBy (actor).asString ().trim ();
    }
    public static Question<String> Title_thaks_for_order() {
        return actor -> TextContent.of (saucedemo_complete_checkout.Title_thaks_for_order).viewedBy (actor).asString ().trim ();
    }*/
}
